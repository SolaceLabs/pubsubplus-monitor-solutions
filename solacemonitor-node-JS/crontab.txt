# compress.sh compresses the processed SEMP response files in the ./store directory
#*/10 * * * * cd /ap/solacemon;./compress.sh
# pingrouter.sh pings the configured Solace PubSub+ instances to get the RTT into InfluxDB
#*/2 * * * * cd /root/solacemon;./pingrouter.sh
# checkAndRestart.sh makes sure that all the essential processes are running
* * * * * cd /ap/solacemon;./checkAndRestart.sh >> log/checkAndRestart.log 2>&1
# sendsempcmds.sh executes the commands in maintenancecmdlist, the primiary motivation is to
# clear the router client stats daily
#0 0 * * * cd /ap/solacemon;./sendsempcmds.sh >> log/sendsempcmds.log 2>&1
# using find command to house-keep the ./store directory
#0 0 * * * find /ap/solacemon/store/*.tgz -mtime +14 -exec rm -f {} \;
# perform scheduled solace backup files archiving
10 0 * * * /ap/solacemon/saveSolaceBackupFiles.sh >> /ap/solacemon/log/saveSolaceBackupFiles.log 2>&1

